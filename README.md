# Chitin Agent Engine

An AI agent runtime — an MCP client that executes tool calls on behalf of an LLM, with every action policy-checked by Chitin before it runs.

## Installation

```bash
pip install -e .
```

## Usage

```bash
chitin-agent
```

## Configuration

See `.chitin/config.yaml` for configuration options.
